server:
  port: 8081

spring:
  application:
    name: menu-service
  datasource:
    url: jdbc:postgresql://localhost:5432/menu_db
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    com.restaurant.menu: DEBUG
